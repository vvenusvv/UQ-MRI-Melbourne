@model FIT5032_Portfolio.Models.Appointment

@{
    ViewBag.Title = "Retrieve Result";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Retrieve Result</h2>

@if (!string.IsNullOrEmpty(ViewBag.Message))
{
    <p>@ViewBag.Message</p>
}

<p>
    @Html.ActionLink("Index", "Index")
</p>

@if (string.IsNullOrEmpty(ViewBag.Message))
{
    <div>
        <h4>Appointment</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Date)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Date)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Time)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Time)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.MRIServiceProvider.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.MRIServiceProvider.Name)
            </dd>

        </dl>
    </div>

    using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Retrieve Result" class="btn btn-outline-dark" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
}